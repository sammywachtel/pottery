# requirements-dev.txt
pytest
pytest-asyncio  # For testing async code
pytest-mock     # Fixture for mocking (unittest.mock wrapper)
httpx           # Required by FastAPI's TestClient
# For debugging with PyCharm
debugpy
# Add any other test-specific dependencies here
